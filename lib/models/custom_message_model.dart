// 메시지 모델
class CustomMessage {
  final String sender;
  final DateTime createdAt;
  final String url;

  CustomMessage({
    required this.sender,
    required this.createdAt,
    required this.url,
  });
}
